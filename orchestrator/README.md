# Orchestrator

This folder documents the extracted orchestrator skill surface.

Layout:

- `core/`: GenServer composition layer that wires the extracted runtime pieces together

The implementation remains in `elixir/lib/symphony_elixir/orchestrator*.ex`.
