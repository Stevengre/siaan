C4Context
title {{TITLE}}
Person(operator, "Operator", "Starts and monitors automated issue execution.")
System_Ext(tracker, "Issue tracker", "Supplies work items and receives state updates.")
System_Ext(codex, "Codex app-server", "Executes agent turns inside issue workspaces.")
System_Ext(repository, "Repository", "Source tree, runtime config, and generated docs.")
System(core, "Repository automation system", "{{SYSTEM_SUMMARY}}")
Rel(operator, core, "Configures and observes")
Rel(core, tracker, "Polls and updates issue state")
Rel(core, codex, "Starts sessions and dispatches turns")
Rel(core, repository, "Reads code, writes docs, manages workspaces")
