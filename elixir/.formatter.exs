# Used by "mix format"
[
  inputs: [
    "{mix,.formatter}.exs",
    "{config,lib,test}/**/*.{ex,exs}",
    "../workflow-engine/{interpreter,mermaid-parser,validate,test}/lib/**/*.{ex,exs}",
    "../workspace/**/*.{ex,exs}",
    "../prompt-engine/**/*.{ex,exs}"
  ],
  line_length: 200
]
