# Used by "mix format"
[
  inputs: [
    "{mix,.formatter}.exs",
    "{config,lib,test}/**/*.{ex,exs}",
    "../workspace/**/*.{ex,exs}",
    "../prompt-engine/**/*.{ex,exs}"
  ],
  line_length: 200
]
